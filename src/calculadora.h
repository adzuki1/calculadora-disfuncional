#ifndef CALCULATOR_H
#define CALCULATOR_H

class Calculator {
public:
        // funções que a calculadora pode fazer
        float add(float a, float b);
        float sub(float a, float b);
        float mul(float a, float b);
        float idiv(float a, float b);

};

#endif
